{
  "name": "quiznest-monorepo",
  "version": "1.0.0",
  "description": "Secure real-time online examination orchestration system",
  "scripts": {
    "dev": "npx concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mkdir -p dist && cp -R frontend/dist/* dist/",
    "db:reset": "npm run db:reset --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
